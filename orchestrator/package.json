{
  "name": "@sherlock/orchestrator",
  "version": "0.0.0",
  "private": true,
  "description": "Modular monolith deployable (RFC §9.2/§9.6): hosts the Bundle Adapters, Fusion Engine, State Manager, Decision/Alert Engine, and Explanation Engine as in-process modules. M0 scaffold only — no domain modules yet.",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --import tsx src/index.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@sherlock/contracts": "*",
    "dotenv": "^16.4.7",
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
