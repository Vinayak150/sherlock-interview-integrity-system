{
  "name": "@sherlock/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "Cross-component schema/type package shared by orchestrator, model-serving, client-agent, and dashboard (RFC §5 repository structure). Empty scaffold in M0 — real contract types land alongside the milestones that need them.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.4"
  }
}
